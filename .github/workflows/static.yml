name: Legacy Pages workflow placeholder

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Explain migration
        run: echo "This legacy workflow has been superseded by .github/workflows/docs.yml"
